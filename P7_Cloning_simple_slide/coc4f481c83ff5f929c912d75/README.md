# we have cloned simple slide in react
1. Why do we need to `import React from "react"` in our files?
Ans :we need to import because we are not using CDN methods and jsx is defined in react

2. If I were to console.log(page) in index.js, what would show up?
Ans: objects

3. What's wrong with this code:
```
const page = (
    <h1>Hello</h1>
    <p>This is my website!</p>
)
```
Ans: No two siblings are render

4. What does it mean for something to be "declarative" instead of "imperative"?
Ans: Declarative is what we want and imperative is how we want

5. What does it mean for something to be "composable"?
Ans: made up of small pieces and joined together to build a project